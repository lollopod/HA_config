homeassistant:
  # Name of the location where Home Assistant is running
  name: Nido
  # Location required to calculate the time the sun rises and sets
  latitude: !secrets latitude
  longitude: !secrets longitude
  # Impacts weather/sunrise data (altitude above sea level in meters)
  elevation: 0
  # metric for Metric, imperial for Imperial
  unit_system: metric
  # Pick yours from here: http://en.wikipedia.org/wiki/List_of_tz_database_time_zones
  time_zone: Europe/Rome
  # Customization file
  customize: !include customize.yaml

# Configure a default setup of Home Assistant (frontend, api, etc)
default_config:

# Uncomment this if you are using SSL/TLS, running in Docker container, etc.
# http:
#   base_url: example.duckdns.org:8123
#added to enable SSL/TLS
#http:
# base_url: https://hwz6fdp0fkttkrms.myfritz.net:8123
# ssl_certificate: /home/homeassistant/.homeassistant/certificate.pem
# ssl_key: /home/homeassistant/.homeassistant/privkey.pem


# Discover some devices automatically
discovery:

# Sensors
sensor:
  # Weather prediction
  - platform: yr

# Text to speech
tts:
  - platform: google_translate

group: !include groups.yaml
automation: !include automations.yaml
script: !include scripts.yaml
xiaomi_aqara:
 discovery_retry: 10
 gateways:
   - key: !secrets xiaomi_gw_key

ios:


media_player:
  - platform: kodi
    host: osmc.local
    port: 80
    username: lollopod
    password: !secrets kodi_password
    enable_websocket: false

meross:
  username: !secrets meross_user
  password: !secrets meross_pw
  scan_interval: 10
  meross_devices_scan_interval: 900

#google_assistant:
#  project_id: via-falloppio
#  api_key: YOUR_API_KEY
#  exposed_domains:
#    - switch
#    - light
#  entity_config:
#    switch.kitchen:
#      name: luce-gateway
#      aliases:
#        - BRIGHT_LIGHTS
#        - ENTRY_LIGHTS
#    light.living_room:
#      expose: false
#      room: LIVING_ROOM
#    group.all_automations:
#      expose: false

#cloud:
#  google_actions:
#    filter:
#      include_entities:
#        - switch.meross_1801160804285825131634298f1111c1

remote:
  - platform: xiaomi_miio
    host: 192.168.188.27
    token: !secrets xiaomi_ir_token

device_tracker:
  - platform: fritz
    host: 192.168.188.1
    username: admin
    password: !secrets frtiz_pw
    new_device_defaults:
      track_new_devices: true
      hide_if_away: false

